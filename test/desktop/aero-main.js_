const assert = require('chai').assert;
describe('screen', function () {
    it('main-card-aero', function () {
        return this.browser
            .url('/')
            .assertView('plain', '.list-unstyled.card-content-list.js-fade-up.in-viewport');
    });
});